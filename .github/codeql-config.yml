name: "MyTipul CodeQL Config"

query-filters:
  # escapeHtml() in email-utils.ts uses /g regex with correct & → < → > → " order.
  # CodeQL flags every call site (~200 alerts) as "incomplete multi-character sanitization"
  # but the implementation is correct. Verified 2026-05-26.
  - exclude:
      id: js/incomplete-multi-character-sanitization
